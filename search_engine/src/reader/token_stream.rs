use std::error::Error;
use std::fs::File;
use std::io::BufReader;
use std::path::Path;

pub struct TokenStream {
    fileName: String,
    buf_reader: BufReader<R>, 
}

impl TokenStream {
    pub fn new(&self, file: &str) {
     
    }
}
